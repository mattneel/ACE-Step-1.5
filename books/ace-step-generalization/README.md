# Generalizing ACE-Step 1.5 (mdBook)

Build locally from this directory:

```bash
cd books/ace-step-generalization
mdbook build
```

Serve with live reload:

```bash
cd books/ace-step-generalization
mdbook serve --open
```
